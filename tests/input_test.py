import math

import nltk
import numpy as np

from typing import Callable, List, Tuple, TypeAlias, ParamSpec, Iterable
from pprint import pprint
import tokenize
from io import StringIO



if __name__ == "__main__":
    # STANDARD_OPERATORS = ["+", "-", "*", "/"]
    # __OPERATORS = {
    #     "log": "np.log",
    #     r"^": "**",
    #     "exp": "np.exp",
    #     "e": "np.e",
    #     "pi": "np.pi",
    # }
    # for op in STANDARD_OPERATORS:
    #     __OPERATORS[op] = op
    # for op in ["sin", "cos", "tan"]:
    #     for var in [op, f"{op}h", f"arc{op}", f"arc{op}h"]:
    #         __OPERATORS[var] = f"np.{var}"
    pass